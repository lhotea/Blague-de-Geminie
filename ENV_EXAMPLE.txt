# Fichier d'exemple pour la configuration
#
# INSTRUCTIONS :
# 1. Copie ce fichier et renomme-le en ".env" (avec le point au début)
# 2. Remplace les valeurs par tes vraies clés
# 3. Le fichier .env est déjà dans .gitignore, donc il ne sera jamais partagé
#
# IMPORTANT : Ne partage JAMAIS ton fichier .env avec quelqu'un !

# ===== API OpenAI =====
OPENAI_API_KEY=sk-ta-cle-api-ici

# ===== OAuth Strava (Multi-utilisateurs) =====
# CLIENT_ID et CLIENT_SECRET de ton application Strava
# Crée une app sur https://www.strava.com/settings/api
STRAVA_CLIENT_ID=ton_client_id
STRAVA_CLIENT_SECRET=ton_client_secret

# URL de redirection OAuth (après autorisation)
# En local: http://localhost:8501
# En production: https://ton-app.streamlit.app
REDIRECT_URI=http://localhost:8501

# ===== OBSOLÈTE (ne plus utiliser) =====
# STRAVA_REFRESH_TOKEN n'est plus nécessaire avec OAuth
# Chaque utilisateur s'authentifie maintenant individuellement
