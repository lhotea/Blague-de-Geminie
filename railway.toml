[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Pas de healthcheck custom - Railway vérifie juste que le port répond
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
